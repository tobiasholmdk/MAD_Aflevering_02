<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.Mad_Aflevering02">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"

        android:theme="@style/Theme.MAD_Aflevering01">

        <activity android:name="com.example.Mad_Aflevering02.Activities.EditActivity"
            android:label="@string/Edit"
            />
        <activity
            android:name="com.example.Mad_Aflevering02.Activities.DetailsActivity"
            android:label="@string/details"
            />
        <activity android:name="com.example.Mad_Aflevering02.Activities.ListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".Services.WeatherService"></service>
    </application>

</manifest>